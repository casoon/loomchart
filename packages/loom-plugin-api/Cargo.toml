[package]
name = "loom-plugin-api"
version = "0.1.0"
edition = "2021"
description = "Public plugin API for LoomChart — traits and stable ABI types for external plugins"
license = "MIT"
repository = "https://github.com/casoon/loom"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
