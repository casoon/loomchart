[package]
name = "loom-plugin-sdk"
version = "0.1.0"
edition = "2021"
description = "SDK for writing LoomChart indicator/strategy/renderer plugins"
license = "MIT"
repository = "https://github.com/casoon/loom"

[dependencies]
loom-plugin-api = { path = "../loom-plugin-api" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[dev-dependencies]
